export const GET = async () => {
  return 'Get users';
};

export const POST = async () => {
  return 'Post user';
};

export const PATCH = async () => {
  return 'Patch user';
};

export const DELETE = async () => {
  return 'Delete user';
};
